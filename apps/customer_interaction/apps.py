from django.apps import AppConfig

class CustomerInteractionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.customer_interaction'
    verbose_name = 'Customer Interaction'